# WUfB Expedite  ·  *Windows KBs*

**What it does:** Expedites Windows **quality updates (KBs)** to KEV-affected devices via Microsoft Graph `windows/updates/deploymentAudiences` → expedite.

> ℹ️ This path is currently **implemented inside the incident-triggered playbook (Path A)** — see `../incident-triggered/KEV-Remediate-LogicApp.json`. This folder is the labeled home for the approach; if/when the WUfB path is split into a standalone Logic App, it lands here.
